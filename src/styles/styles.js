import { StyleSheet, StatusBar } from "react-native";

// export default StyleSheet.create({
//   container: {
//     paddingTop: StatusBar.currentHeight || 0, // Добавляем отступ сверху
//   },
// });